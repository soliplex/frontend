name: soliplex_agent
description: Pure Dart agent orchestration for Soliplex AI runtime.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  ag_ui:
    git:
      url: https://github.com/soliplex/ag-ui.git
      path: sdks/community/dart
  collection: ^1.18.0
  meta: ^1.9.0
  signals_core: ^6.2.0
  soliplex_client:
    path: ../soliplex_client
  soliplex_logging:
    path: ../soliplex_logging

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.24.0
  very_good_analysis: ^10.0.0
