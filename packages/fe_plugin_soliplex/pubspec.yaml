name: fe_plugin_soliplex
description: MontyPlugin that bridges sandboxed Python to the Soliplex server API.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  dart_monty:
    git:
      url: https://github.com/runyaga/dart_monty.git
      ref: 25bb7de
  meta: ^1.9.0
  soliplex_client:
    path: ../soliplex_client

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.24.0
  very_good_analysis: ^10.0.0
