name: soliplex_design
description: Core design system for Soliplex — tokens, theme factories, and shared visual primitives.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Reused by golden tests so the snapshot matches what `flutter run` shows.
  soliplex_design_example:
    path: example
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
