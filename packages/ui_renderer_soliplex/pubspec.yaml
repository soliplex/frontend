name: ui_renderer_soliplex
description: SoliplexUiRenderer and DebugUiPanel — Flutter implementation of UiRenderer for Soliplex.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown_plus: ^1.0.7
  meta: ^1.9.0
  signals_flutter: ^6.3.0
  ui_plugin:
    path: ../ui_plugin

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0
