include: package:very_good_analysis/analysis_options.6.0.0.yaml

analyzer:
  exclude:
    # Hand-maintained wire-format schema mirrors: double-quoted keys and
    # field layout intentionally diverge from the lint rules.
    - lib/src/schema/agui_features/*

linter:
  rules:
    public_member_api_docs: true
